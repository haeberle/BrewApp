<UserControl
    x:Class="BrewApp.Controls.Brewery.TemperatureSimulator"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BrewApp.Controls.Brewery"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Slider Margin="10,0,10,0" Grid.Row="0" Grid.Column="0" x:Name="vesselTempSlider" Header="Vessel" Minimum="20" Maximum="110" ValueChanged="vesselTemp_ValueChanged"/>
        <Slider Margin="10,0,10,0" Grid.Row="0" Grid.Column="1" x:Name="mashTempSlider" Header="Mash" Minimum="20" Maximum="110" ValueChanged="mashTemp_ValueChanged"/>
    </Grid>
</UserControl>
